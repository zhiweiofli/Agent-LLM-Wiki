# Claude Skill Mirror

This directory mirrors `.agents/skills/` for Claude Code compatibility.

Keep the two directories semantically aligned. In the public template, `.agents/skills/` is the primary source.
